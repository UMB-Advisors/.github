# Temporary: proves the reusable workflows parse and their input expressions resolve.
name: selftest
on:
  workflow_dispatch:
jobs:
  node:
    uses: ./.github/workflows/node-ci.yml
    with:
      node-version: '22'
      install: "echo install-ok"
      lint: "echo lint-ok"
      build: ""
      test: "echo test-ok"
  python:
    uses: ./.github/workflows/python-ci.yml
    with:
      python-version: '3.12'
      install: "echo install-ok"
      lint: "echo lint-ok"
      format-check: ""
      test: "echo test-ok"
